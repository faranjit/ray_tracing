[package]
name = "ray_tracing"
version = "0.1.0"
edition = "2024"

[profile.dev-fast]
inherits = "release"
lto = false
codegen-units = 16

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

[dependencies]
fastrand-contrib = "0.1.0"
image = "0.25.10"
rayon = "1.12.0"
